"""
Review and Summary
1. Numeric
    Decimal     1.3 == 1.1+1.2
    Fraction
    math        log, abs, sin, cos
    random      randomint, randomrange, shuffle
2. List
    create/add/update/remove, built-in, function/api
3. Tuple
    create, auto-package, auto-unpackage, function/api, built-in
4. String
    api, strip(), split(), join(), replace(), find(), index()
5. Set
    create, update, add, remove
    set operation,  Union, Intersection, Difference, Symmetric Difference
    api, function
6. Dictionary
    create, update, add, remove
    api, function, module (collections, operator), zip()
    built-in, sorted, items(), keys(), values()
"""